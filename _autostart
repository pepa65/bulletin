# /home/pi/.config/lxsession/LXDE/autostart

#@lxpanel --profile LXDE
#@pcmanfm --desktop --profile LXDE
#@xscreensaver -no-splash

# No screensaver
@xset s off

# No powermanager
@xset -dpms

# No screen blanking
@xset s noblank

# No cursor
@unclutter -idle 0

# Start php webserver (for refresh.php)
@/home/pi/git/bulletin/websrv log

# Prevent post-crash dialogs
@sed -i 's/"exited_cleanly": false/"exited_cleanly": true/' /home/pi/.config/chromium/Default/Preferences

# Start browser in kiosk-mode, with no pinch allowed, allow file reads,
#  no update-available message ever, no error dialogs, and unencumbered.
@chromium-browser %URL --kiosk --disable-pinch --allow-file-access-from-files --check-for-update-interval=31536000 --noerrdialogs --incognito &
